package com.city2farmer;

import android.app.AlertDialog;
import android.content.Context;

public class FarmAlertDialog extends AlertDialog {

    protected FarmAlertDialog(Context context) {
        super(context);
    }
}
